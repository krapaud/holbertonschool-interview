# Sorted array to AVL

This project builds a balanced AVL tree from a sorted array without using
rotations.

The middle element becomes the root. The same idea is then applied
recursively to the left and right halves of the array.
